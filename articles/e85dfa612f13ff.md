---
title: "[Flutter]ListItemが見えなくなっても状態を維持する方法"
emoji: "🎉"
type: "tech" # tech: 技術記事 / idea: アイデア
topics: []
published: false
---

## はじめに

ListViewのListItemが見えなくなっても状態を維持する方法を紹介します。

## やりたいこと

ListViewのListItemが見えなくなっても状態を維持する方法を紹介します。

## やり方

AutomaticKeepAliveClientMixinを使用します。

```dart
class MyListItem extends StatefulWidget {
  const MyListItem({super.key});

  @override
  State<MyListItem> createState() => _MyListItemState();
}

class _MyListItemState extends State<MyListItem> with AutomaticKeepAliveClientMixin {
  @override
  bool get wantKeepAlive => true;

  @override
  Widget build(BuildContext context) {
    super.build(context);
    return Container();
  }
}
```

これだと必ずListItemが見えなくなっても状態を維持します。こうした使い方をすることはないと思います（ずっと状態を維持することになってしまうので）。

ということで具体的にありそうなケースを考えてみます。

### タップしてから一定時間だけ状態を維持する

```dart
```

## おわりに


